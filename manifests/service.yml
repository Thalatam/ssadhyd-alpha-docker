apiVersion: v1
kind: Service
metadata:
    name: "ssadhydalphak8suseast2"
    labels:
        app: "ssadhydalphak8suseast2"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "ssadhydalphak8suseast2"